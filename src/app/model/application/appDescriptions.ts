/**
 * This file contains static description text for apps.
 * I tried (really tried) to put these into .md files and load them dynamically but `import("./md/...")` does not like me. :(
 */

export const jsonViewerDescription = `
The _JSON Viewer_ app is a handy tool that I use regularly.

It parses the text that you provide and if text is valid JSON, offers ways to auto-indent or minimize it.

In the Viewer tab, a nicely configured tree structure will be generated in order for you to visualize your JSON object.
`;

export const homeAppDescription = `
The _Home_ app is the home page of _labs.jhuang.ca_
`;
